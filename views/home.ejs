<%- include('partials/header'); -%>
    <h1>Home</h1>
    <p> <%= startingContent %> </p>
   <% blogs.forEach(function(blogs){ %>
        <h1><%= blogs.title %> </h1>
        <p><%= blogs.body %></p>
    <% }); %>
<%- include('partials/footer'); %>    